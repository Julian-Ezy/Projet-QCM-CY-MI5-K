#include <stdio.h>

int main() {
    int ta_mere;
    printf("j'en peux plus go valo");
    scanf("%s", &ta_mere);
    return 0;
}
git config --global user.name "Julian-Ezy" ; git config --global user.email "julian-ezy@example.com"